# DevLauncher Test Checklist

## 1. 整合包导入 — client-overrides 支持
- [ ] 导入包含 `client-overrides/` 的 .mrpack 文件（如 NON 整合包）
- [ ] 验证 `client-overrides/mods/` 下的 jar 文件被正确解压到 `versions/<name>/mods/`
- [ ] 验证 `client-overrides/config/` 下的配置文件被正确解压
- [ ] 验证 `client-overrides/needsofnature/` 等子目录资源包被正确解压
- [ ] 导入后启动游戏，确认所有模组加载正常（无缺失模组）
- [ ] 对比 HMCL 导入同一整合包的 mods 目录，确认文件一致

## 2. 整合包格式检测
- [ ] 导入 `client-overrides/modrinth.index.json` 的 mrpack，确认被识别为 Modrinth 格式
- [ ] 导入 `overrides/modrinth.index.json` 的 mrpack，确认被识别为 Modrinth 格式
- [ ] 导入 `modrinth.index.json` 在根目录的 mrpack，确认被识别为 Modrinth 格式

## 3. 导入弹窗 UI
- [ ] 打开导入弹窗，确认显示整合包信息（名称、MC版本、加载器、模组数量）
- [ ] 点击导入后，确认显示 HMCL 风格进度界面
- [ ] 进度界面显示步骤图标（→）、步骤标题、步骤副标题（含文件计数）
- [ ] 文件列表显示正在下载的文件（带进度条），已下载的文件显示 ✓
- [ ] 下载完成后步骤图标变为 ✓
- [ ] 导入过程中无法关闭弹窗（modpackImporting 锁定）
- [ ] 导入完成后自动关闭弹窗，版本列表刷新

## 4. 导入整合包按钮图标
- [ ] 版本页面右上角「导入整合包」按钮显示正确的 SVG 图标（箭头+碗状）
- [ ] 图标与文字间距正常，不拥挤

## 5. 侧边栏 Logo
- [ ] 侧边栏顶部 D_ logo 的 D 和 _ 之间有足够间距
- [ ] Logo 在 48x48 尺寸下清晰可辨

## 6. 导入进度计数（2026-09-25 修复）
- [ ] 导入大模组包（>100 模组），确认模组下载阶段显示 `X/Y` 后，提取/配置阶段**不会**回落成 `85/100` 之类
- [ ] 全程计数单调递增，任何时刻不出现"从 1 重新开始"
- [ ] 下载有失败文件时，失败项标红 ✗，计数显示 `成功数/总数`
- [ ] 导入进行中「取消」按钮置灰显示「导入中...」，弹窗无法关闭
- [ ] 导入进行中再次点击「导入整合包」按钮，提示"已有整合包正在导入中"
- [ ] 模拟导入失败（如断网），弹窗正常显示失败结果且不卡死，可再次导入

## 7. 并发下载与原版安装
- [ ] 观察日志确认模组下载并发为 16 线程
- [ ] 确认下载 CDN 请求头不含 `x-api-key`
- [ ] 全新版本导入时，原版安装阶段副标题依次出现"下载/解压/安装 Minecraft"（不再全程停在 91%）
- [ ] `redownloadModpackMods`（重新下载整合包模组）侧边栏有进度，完成后回到 idle

## 8. 已有功能回归
- [ ] 版本列表正常显示
- [ ] 模组管理页面正常加载模组列表
- [ ] 模组下载/安装功能正常
- [ ] 游戏启动功能正常
- [ ] 版本隔离切换正常
- [ ] 主题切换正常

## 9. 字节级实时进度（2026-09-25 修复）
- [ ] 导入模组包时，文件列表里下载中的条目进度条**连续**从 0% 走到 100%（不再 0→100 跳变）
- [ ] 弹窗整体进度条随字节推进平滑增长，副标题显示 `… · NN%`
- [ ] 同时下载多个文件时，多个条目各自推进，百分比更新不卡顿（0.15s 节流）
- [ ] 侧边栏下载面板计数显示 `X/Y · NN%`，文件名旁显示该文件当前百分比
- [ ] 下载完成后文件条变绿 ✓，整体进度与计数一致
- [ ] CurseForge 包导入同样有 per-file 进度（本版新增 file_states）

## 10. Forge/NeoForge 安装与显示（2026-09-25 修复）
- [ ] 导入 Forge mrpack（如 The Other Side-1.20.1）后，加载器页显示 Forge 及其版本号
- [ ] 版本 JSON：`jar` = 原版版本、`mainClass` = `BootstrapLauncher`（不再是 FMLClientTweaker 占位）、libraries 非空
- [ ] 启动命令干跑包含原版 client jar 与 forge 库（不缺 `1.20.1.jar`）
- [ ] 全新导入（本地无原版 1.20.1）时自动补装原版 json/jar/资源
- [ ] 加载器页对整合包版本直接安装加载器（装进包 JSON，而非原版父版本）
- [ ] Fabric / Quilt / OptiFine / NeoForge 安装不再报 `unexpected keyword 'installer_url'`
- [ ] NeoForge 安装后 mainClass 生效（从安装器 version.json 合并）
- [ ] 半安装状态（有 JSON 无 client jar）重试导入会补齐原版
- [ ] 导入失败的加载器安装在结果页显示 warning，模组下载成果不丢

## 11. 模组列表加载速度（2026-09-25 优化）
- [ ] 60+ 模组的版本切到模组页，列表快速出现（8 线程并行解析）
- [ ] 二次进入模组页走 `modcache.json` 缓存，几乎瞬时
- [ ] 替换/新增/删除 jar 后列表内容正确（缓存键含 size/mtime）
- [ ] 图标与模组元数据（fabric.mod.json / mcmod.info）显示正常

## 12. 版本列表幽灵原版（2026-09-25 修复）
- [ ] 导入 1.21.11 整合包后，已安装列表只显示整合包本体（不再多出 1.21.11）
- [ ] 下载页中原版 1.21.11 显示为未安装（无"已安装"角标）
- [ ] 删除整合包后列表无连带变化（其余版本一个不少）
- [ ] 下载页手动安装/修复该原版 → 出现在已安装列表（写入 cfg 转正）
- [ ] 带 `devlauncher.cfg` 的版本（如 1.12.2、1.4.5）与整合包显示不受影响
- [ ] 版本隔离开关、启动功能回归正常

## 13. 导入进度体验（2026-09-25 修复）
- [ ] 导入时在文件列表往上滚动，刷新期间不再被拽回底部
- [ ] 下载面板（重新下载模组等）的文件列表同样保持滚动位置
- [ ] 进度条随字节平滑推进，最后几个大文件期间不卡在 98-99% 蠕动
- [ ] 下载完成后进度条继续走阶段：提取 → 创建版本配置 → 安装 Minecraft → 安装加载器 → 100%（不再钉死）
- [ ] 某个文件下载失败重试时，整体进度不永久停在 98%，收尾阶段照常推进
- [ ] 重试期间该文件在列表里的进度数字**不回落**（停在已到达位置，追上后继续），侧栏整体进度条同样不回退
- [ ] 某文件最终失败(error)时，侧栏整体进度条不因此下跌
- [ ] 文件列表底部"还有 N 个文件"随下载**递减**（不再增长），全部完成后整行消失
- [ ] 补装原版/安装加载器阶段副标题不再追加"— 62/62"这类模组计数
- [ ] MultiMC 包（先解压后下载）进度条不倒退

## 14. 模组页响应与卡顿（2026-09-25 修复）
- [ ] 点击"禁用"后该行**立刻**变灰、按钮立刻变"启用"（不等后端返回），后端返回后列表不闪不跳、滚动位置保持
- [ ] 快速连点启用/禁用（在返回前再点）不报"模组不存在"，最终状态与文件名一致（.disabled 后缀正确）
- [ ] 启用/禁用往返明显变快（命中缓存，无 Modrinth 图标网络请求，不再 3.7s）
- [ ] 大模组列表（300+）刷新不冻结 UI（分批渲染），刷新前后滚动位置保持
- [ ] 删除模组行立即消失，随后返回的列表与之一致
- [ ] 版本模组/版本世界/版本资源包页切换时先出现骨架屏（不再白屏等待）
- [ ] 快速切换版本或重复进入模组页不会并发扫描（日志仅一次加载，结果不串版本）
- [ ] 删除世界/资源包、启用/禁用/删除模组期间界面不卡（文件操作在后台线程）
- [ ] 全局模组页与版本模组页的启用/禁用/删除回归正常

## 15. 收起（弹窗原位缩小）/ 转圈连续性（2026-09-25 重定义，替代"窗口细条"）
- [ ] 顶栏不再有"收起"按钮，窗口不会变形成细条（旧方案已整体移除）
- [ ] 导入开始前点弹窗"收起" → toast 提示"导入开始后才能收起"，弹窗不变
- [ ] 导入进行中点"收起" → 弹窗**原位**收成下载器样式小面板：左侧"下载模组"、右侧"N/M · pct%"、细进度条、转圈文件列表、底部阶段消息 + "展开"按钮
- [ ] 收起状态下进度继续刷新：计数与百分比递增不回退，文件列表滚动位置保持
- [ ] 点"展开" → 恢复完整弹窗（标题/信息/进度/底栏与收起前一致）；收起/展开连点无异常（幂等）
- [ ] 内置下载面板（安装版本/重新下载模组）的转圈圆圈在进度更新时**连续旋转，角度不重置**
- [ ] 收起小面板里的转圈同样连续（列表每 0.15s 重建也不重置）
- [ ] 转圈完成（done ✓）/失败（✗）图标正确替换，不再旋转
- [ ] 导入完成（成功或失败）处于收起状态 → 自动展开，能看到结果提示，弹窗状态正常
- [ ] 导入中遮罩点击不关闭弹窗，取消按钮显示"导入中..."且禁用
